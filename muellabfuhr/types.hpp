#include <vector>
#include <map>
#include <set>

typedef std::vector<std::map<int, int>> adj_map;
typedef std::vector<std::set<int>> adj_set;

typedef std::map<int, std::map<int, int>> map_2d;

typedef std::vector<std::vector<int>> matrix_2d;
